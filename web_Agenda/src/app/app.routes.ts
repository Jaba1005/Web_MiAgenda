import { Routes } from '@angular/router';
import { AgendaComponent } from './components/agenda-component/agenda-component';

export const routes: Routes = [
    { path: 'inicio', component: AgendaComponent }
];
